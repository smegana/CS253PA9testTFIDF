#ifndef READINGLEVEL_H
#define READINGLEVEL_H

#include <vector>
#include <string>

using namespace std;


std::vector<double> wordletters(std::vector<std::string>);
double L(std::vector<std::string>);
int countsentences(std::vector<std::string>);
double S(std::vector<std::string>);
double CL(std::vector<std::string> words);

#endif
